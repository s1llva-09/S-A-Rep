# Copie para ".env" e preencha após criar o projeto no Supabase.
# Enquanto estiver vazio, o site usa o conteúdo de fallback (continua funcionando).
# A anon key é pública por design — a segurança vem das policies (RLS).

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
